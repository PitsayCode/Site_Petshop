# Ambiente local
.claude/
.vercel/
node_modules/
.env
.env.*

# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Materiais do cliente: contrato (CPF, endereço), apresentação, backup.
# Ficam só no computador, nunca no GitHub nem na hospedagem.
_cliente/
